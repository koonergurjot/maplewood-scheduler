# Task Backlog

## Identified Discrepancies

- [ ] Replace the placeholder screenshots referenced in `README.md` (`docs/media/screenshot-*.png`) with actual captures of the current UI so the documentation matches the product.
- [ ] Publish or document a hosted demo URL; `README.md` now points to the checked-in `index.html` as a temporary stand-in.
- [ ] Expand the analytics API documentation referenced in the README so the Express server endpoints and authentication flow are clear to integrators.
